Array.prototype.map2 = function(callback){
    var arrayLength = this.length
    for(i = 0 ; i<arrayLength;++i){

    }
    course =[
        'java',
        'php',
        'html'
    ]
    console.log.map2(function(course,index) {
        console.log(index,course)
    })
}

